# Development environment
# Vite proxies all /api/* requests to this target (avoids CORS in local dev)
VITE_PROXY_TARGET=http://localhost:8080

# Leave empty so the browser calls /api/* (proxied by Vite dev server above)
VITE_API_URL=
